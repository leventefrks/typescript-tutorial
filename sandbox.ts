let names = ['john', 'mary', 'luigi'];

names.push('julia');

let numbers = [1, 2, 5, 7, 2];

numbers.push(11);

let ninja = {
  name: 'mario',
  belt: 'black',
  age: 30,
};

ninja.age = 66;

// explicit types
let character: string;
let number: number;
let isLoggedIn: boolean;

// arrays

// union types

// objects
